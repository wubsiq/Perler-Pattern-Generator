# 🧪 模块测试指南

## 📋 测试概述

您已经成功完成了豆师傅项目的模块化拆分！以下是完整的测试指南，帮助您验证所有模块是否正常工作。

---

## 🚀 快速开始

### 方法 1：使用模块测试页面（推荐）

1. 直接在浏览器中打开：`test-modules.html`
2. 点击 **"▶ 运行所有测试"** 按钮
3. 查看测试结果

### 方法 2：直接测试主应用

1. 在浏览器中打开：`index.html`
2. 进行完整的功能测试

---

## 📦 模块清单与测试要点

### 1. ✅ 基础模块（已验证）

| 模块 | 文件 | 测试要点 |
|------|------|----------|
| **StateManager** | `modules/StateManager.js` | 状态管理是否正常 |
| **Pixelator** | `modules/Pixelator.js` | 像素化处理是否工作 |
| **PerlerGenerator** | `modules/PerlerGenerator.js` | SVG导出、图例生成 |

### 2. 🆕 新创建模块

| 模块 | 文件 | 测试要点 |
|------|------|----------|
| **ImageUploader** | `modules/ImageUploader.js` | 图片上传、拖拽、验证 |
| **DownloadManager** | `modules/DownloadManager.js` | PNG/SVG下载、文件命名 |
| **SnapshotManager** | `modules/SnapshotManager.js` | 快照保存、历史记录 |
| **CustomEditor** | `modules/CustomEditor.js` | 画笔、橡皮擦、填充工具 |
| **SmartOptimizer** | `modules/SmartOptimizer.js` | 智能颜色优化 |
| **CarveSplitter** | `modules/CarveSplitter.js` | 雕刻分裂功能 |
| **PixelArtProcessor** | `modules/PixelArtProcessor.js` | 像素化渲染、网格 |
| **ColorManager** | `modules/ColorManager.js` | 颜色统计、转换 |
| **CanvasRenderer** | `modules/CanvasRenderer.js` | 画布渲染、珠子绘制 |
| **UIController** | `modules/UIController.js` | UI协调、事件处理 |

---

## 🧪 详细测试步骤

### 测试 1：基础功能（必须通过）

**操作步骤：**

1. 打开 `index.html`
2. 上传一张图片（支持 JPG/PNG/GIF）
3. 调整像素大小和尺寸
4. 点击"渲染拼豆图纸"
5. 选择"SVG"格式，点击"下载"

**预期结果：**
- ✅ 图片上传成功
- ✅ 像素化预览正常
- ✅ 拼豆图纸渲染成功
- ✅ SVG导出包含颜色统计、图例、水印

### 测试 2：模块测试页面

**操作步骤：**

1. 打开 `test-modules.html`
2. 点击"▶ 运行所有测试"
3. 观察每个模块的状态

**预期结果：**
- ✅ 大部分模块显示"✓ 正常"（绿色）
- ✅ 没有"✗ 错误"（红色）
- ✅ 测试日志显示正常

### 测试 3：下载功能

**操作步骤：**

1. 在主应用中渲染拼豆图纸
2. 选择"PNG"格式，点击下载
3. 选择"SVG"格式，点击下载
4. 下载像素图

**预期结果：**
- ✅ 所有下载功能正常
- ✅ 文件名自动编号
- ✅ 图片质量良好

### 测试 4：自定义编辑

**操作步骤：**

1. 进入"自定义编辑"标签
2. 测试画笔工具
3. 测试橡皮擦工具
4. 测试填充工具
5. 测试撤销功能

**预期结果：**
- ✅ 所有编辑工具工作正常
- ✅ 撤销功能正常

### 测试 5：快照功能

**操作步骤：**

1. 进行几次编辑操作
2. 点击"保存快照"
3. 查看快照列表
4. 恢复一个快照

**预期结果：**
- ✅ 快照保存成功
- ✅ 快照恢复正常
- ✅ 快照列表显示正常

### 测试 6：智能优化

**操作步骤：**

1. 点击"🧠 智能优化"按钮
2. 查看优化建议
3. 应用优化

**预期结果：**
- ✅ 优化建议显示正常
- ✅ 优化应用成功

### 测试 7：雕刻分裂

**操作步骤：**

1. 进入雕刻分裂模式
2. 点击色块进行分裂
3. 测试分裂全部功能

**预期结果：**
- ✅ 分裂功能正常
- ✅ 块渲染正常

---

## 🔍 调试指南

### 查看控制台错误

1. 在浏览器中按 `F12` 打开开发者工具
2. 切换到 **Console** 标签
3. 刷新页面，查看是否有错误

### 检查模块加载

在浏览器控制台中输入：

```javascript
// 检查某个模块是否存在
console.log('DownloadManager:', typeof downloadManager);
console.log('ImageUploader:', typeof imageUploader);
console.log('ColorManager:', typeof colorManager);

// 查看所有全局变量
console.log(window);
```

### 常见问题与解决方案

#### 问题 1：模块未加载

**现象：** 控制台报错 `xxx is not defined`

**解决方案：**
- 检查 `index.html` 中的脚本引入顺序
- 确保所有模块文件路径正确
- 检查浏览器是否有缓存（Ctrl+F5 强制刷新）

#### 问题 2：功能异常

**现象：** 某个功能不工作

**解决方案：**
- 检查控制台是否有错误
- 确认对应的模块已正确引入
- 检查相关的 DOM 元素 ID 是否匹配

#### 问题 3：SVG 导出问题

**现象：** SVG 导出缺少内容

**解决方案：**
- 确认 `PerlerGenerator` 模块正确加载
- 检查颜色统计数据是否正确传递
- 验证图例位置设置

---

## 📊 测试检查清单

使用以下清单来跟踪您的测试进度：

- [ ] 模块测试页面正常加载
- [ ] 所有模块测试通过
- [ ] 图片上传功能正常
- [ ] 像素化渲染正常
- [ ] 拼豆图纸渲染正常
- [ ] PNG 下载正常
- [ ] SVG 下载正常（含颜色统计、图例）
- [ ] 自定义编辑工具正常
- [ ] 快照功能正常
- [ ] 智能优化功能正常
- [ ] 雕刻分裂功能正常
- [ ] 无控制台错误

---

## 🎯 验收标准

### 基础要求（必须满足）
1. ✅ 主应用能正常打开
2. ✅ 图片上传功能正常
3. ✅ PNG/SVG 导出功能正常
4. ✅ SVG 导出包含完整内容（颜色统计、图例、水印）
5. ✅ 无致命错误（红色错误）

### 进阶要求（最佳有）
1. ✅ 所有模块测试通过
2. ✅ 所有功能正常工作
3. ✅ 无警告（黄色警告）
4. ✅ 性能良好，无明显卡顿

---

## 📝 测试报告模板

完成测试后，记录以下信息：

```
测试日期：
测试人员：
浏览器版本：
操作系统：

测试结果：
- 基础功能：[ 通过 / 部分通过 / 失败 ]
- 模块测试：[ 通过 / 部分通过 / 失败 ]
- 完整功能：[ 通过 / 部分通过 / 失败 ]

发现的问题：
1. 
2. 
3. 

建议：
1. 
2. 
```

---

## 🚀 下一步

测试完成后，您可以：

1. **修复发现的问题** - 解决测试中发现的问题
2. **继续重构** - 将 app.js 的功能迁移到新模块
3. **添加新功能** - 基于新模块架构开发新功能
4. **优化性能** - 优化模块性能和用户体验

---

## 💡 提示

- 如果在测试中遇到问题，请先查看浏览器控制台
- 强制刷新页面（Ctrl+F5）清除缓存
- 可以使用 `test-modules.html` 进行模块级别的快速测试
- 主应用保持原样，确保现有功能不受影响

祝测试顺利！🎉
